name: CD
permissions:
   contents: read
   id-token: write
   packages: write
   actions: read
   checks: write
   deployments: write
   pull-requests: write
   statuses: write
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: nehaatwork0
          password: docker005
      - name: Build Docker images
        run: |
          docker build -t nehaatwork0/chat-app-frontend ./frontend
          docker build -t nehaatwork0/chat-app-backend ./backend

      - name: Push Docker images
        run: |
          docker push nehaatwork0/chat-app-frontend
          docker push nehaatwork0/chat-app-backend

      # - name: Install Render CLI
      #   run: |
      #     curl --request GET \
      #     --url 'https://api.render.com/v1/services?limit=20' \
      #     --header 'Accept: application/json' \
      #     --header 'Authorization: Bearer {{SECRETS.RENDER_TOKEN}}'

      - name: Install Render CLI
        run: |
          curl -fsSL https://cdn.render.com/cli/latest/install.sh | bash
          export PATH=$HOME/.render/bin:$PATH

      - name: Deploy to Render
        env:
          RENDER_API_KEY: rnd_UIYwfwEXphBnf6sFZIMPcDf7whPf
        run: |
          render deploy service tea-d3ha4logjchc73abknmg --api-key rnd_UIYwfwEXphBnf6sFZIMPcDf7whPf




